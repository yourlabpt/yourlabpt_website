# Dependencies
node_modules/
package-lock.json
yarn.lock

# Environment variables
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*

# Build
dist/
build/
test-results/
playwright-report/

# Inquiries (keep example only)
server/inquiries/*
!server/inquiries/.gitkeep

# Runtime content
server/project-showcase.json

# Platform hybrid storage (local runtime)
projects/data/platform.db
projects/data/platform.db-*
projects/data/blobs/
projects/data/snapshots/
projects/data/store.json.bak

# Encrypted provider credentials and the local key that unwraps them.
# The rest of projects/data/ is tracked, so these must never live there unignored.
projects/data/*.secret.json
projects/data/.secret-key

# Digitalize Portugal sales app (local runtime)
server/data/digitalizept.db
server/data/digitalizept.db-*
server/data/digitalizept-contracts/
server/data/digitalizept-work/

# Agent skills installed per-repo (npx skills add) — not ours to commit
projects/.agents/
projects/.claude/skills/
